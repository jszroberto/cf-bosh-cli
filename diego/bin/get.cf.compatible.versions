#!/bin/bash

usage()
{
cat << EOF
usage: $0 VERSION

This gives you the compatible version of Diego, Garden linux, etc for a given CF version

EOF
}

if [[ $# != 1 ]]
then
	usage
	exit 1
fi 

echo "$(generate.compatibility.table | grep "^${1}" | sort | head -n 1)"
