#!/bin/bash

# detect_new_cf_major_release(){
# 	get_latest_cf_release
# 	oldversion=$cf_version
# 	update_cf_release &>/dev/null
# 	get_latest_cf_release
# 	newversion=$cf_version
# 	if [[ "$oldversion" == "$newversion" ]]
# 	then 
# 		trigger
# 	fi
# }

# trigger(){
# 	echo "New CF VERSION"
# 	newrow=$(generate_table | grep $newversion | sort | head -n 1)
# 	echo $newrow
# }

echo "$(generate.compatibility.table | grep "^${1}" | sort | head -n 1)"
